/** @deprecated Import from memberContactColumns — kept for backward-compatible import paths. */
export {
  MEMBER_CONTACT_COLUMNS as MASTER_CONTACT_COLUMNS,
  MEMBER_CONTACT_COLUMN_LABELS as MASTER_CONTACT_COLUMN_LABELS,
  buildContactEditFields,
  buildContactEditFieldSections,
  orderContactColumns,
  type ContactData,
  type ContactEditField,
  type ContactEditFieldSection,
  type ContactEditBuildOptions,
  type ContactFieldInputType,
  type ContactSelectOption,
} from './memberContactColumns';
